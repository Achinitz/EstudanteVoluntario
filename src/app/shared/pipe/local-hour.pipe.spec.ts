import { LocalHourPipe } from './local-hour.pipe';

describe('LocalHourPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalHourPipe();
    expect(pipe).toBeTruthy();
  });
});
