import { NomeUsuarioPipe } from './nome-usuario.pipe';

describe('NomeUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new NomeUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
